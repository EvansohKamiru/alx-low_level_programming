#include <stdio.h>

/**
* main - a program to print 0 - 10 in a newline
*
* Return: Always success
*/
int main(void)
{
	char num;

	for  (num = 0; num <= 10; num++)
		printf("%d\n", num);
return (0);
}
